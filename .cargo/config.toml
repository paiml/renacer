# ZRAM build acceleration
[build]
incremental = true
target-dir = "/mnt/zram/targets/renacer"

[target.x86_64-unknown-linux-gnu]
rustflags = ["-C", "target-cpu=native"]

[env]
RUST_BACKTRACE = "1"

[alias]
t = "test"
c = "check"
b = "build --release"
cl = "clippy --all-targets -- -D warnings"
