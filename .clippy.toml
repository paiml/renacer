# Clippy configuration for renacer
# Enforces project quality standards

disallowed-methods = [
    { path = "std::option::Option::unwrap", reason = "Use .expect() with a meaningful message or proper error handling" },
    { path = "std::result::Result::unwrap", reason = "Use .expect() with a meaningful message or proper error handling" },
]

# Cognitive complexity threshold
cognitive-complexity-threshold = 25
